# API Configuration (МойСклад)
API_URL=https://api.moysklad.ru/api/remap/1.2/report/stock/all

# Вариант 1: Аутентификация по токену (Bearer Token)
# Раскомментируйте если используете токен:
# API_TOKEN=your_access_token_here

# Вариант 2: Аутентификация Basic Auth (логин и пароль)
# Раскомментируйте если используете логин/пароль:
API_LOGIN=your_login_here
API_PASSWORD=your_password_here

# Момент времени для запроса остатков (опционально)
# Формат: YYYY-MM-DD HH:MM:SS
# Если не указан, используется текущая дата/время
# API_MOMENT=2025-12-01 07:00:00

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=stock_sync
DB_USER=root
DB_PASSWORD=your_password_here

# Application Configuration
PORT=3000
CRON_SCHEDULE=0 0 * * *
LOG_FILE=logs/sync.log

# Timezone for cron (optional)
TZ=Europe/Moscow

